---
title: "Rapport d'analyse des données Carine Roche"
author: "Nicolas Bressoud"
date: "5/15/2020"
output: pdf_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
source("script_carine_solo.R")
```

## Statistiques Descriptives

L'échantillon est constitué de `r data3$n_total` participants ( `r data3$n_filles` filles) âgés d'environ 10 ans (données sur l'âge non disponibles). Les participants sont répartis en `r nlevels(data2$classe)` groupes : `r levels(data2$classe)[1]` et `r levels(data2$classe)[2]`. Les deux groupes ont suivi le programme d'intervention au même moment. La *table 1* synthétise les statistiques de l'échantillon: <!-- j'aurais aimé référencer automatiquement la table 1... -->
<!-- maintenant je veux un tableau APA avec en en-tête classe, n, filles, garcons. Je ne trouve pas comment le générer alors je le construis.
-->

```{r table_sample, echo=FALSE}
library(knitr)
kable(data4, caption="résumé des données de l'échantillon",label="sample")
```

```{r table_sample_bis, echo=FALSE}

kable(sample2, caption="résumé des données de l'échantillon",label="sample")
```


Les participants ont répondu à un questionnaire à deux reprises. Les questions concernaient...il y avait x questions. Les partipants pouvaient indiquer... exemples. 
La cohérence interne du questionnaire était de ... ...

La *table 2* synthétise les moyennes et écart-types de chaque temps de mesure.
<!-- la mise en forme est pas du tout ce que je veux car les variables sont brutes et j'ai pas les labels des catégories. -->
```{r table_descr, echo=FALSE}
kable(data6, caption="résumé des statistiques descriptives",label="descr")
```

La *figure 1* propose une visualisation des données descriptives :

```{r plot_descr, echo=FALSE}
plot_desc
plot_desc2
```


